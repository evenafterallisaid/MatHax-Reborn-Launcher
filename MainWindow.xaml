<Window x:Class="MatHax.Reborn.Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="MatHax Reborn Launcher"
        Width="980" Height="620" MinWidth="820" MinHeight="540"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        ResizeMode="CanResize" Background="Transparent" AllowsTransparency="True">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="46" ResizeBorderThickness="7" CornerRadius="16" GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>

    <Border Background="{StaticResource BackgroundBrush}" CornerRadius="16" BorderBrush="#2A2032" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="46" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <TextBlock Text="MATHAX REBORN" Margin="18,0,0,0" VerticalAlignment="Center" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource MutedBrush}" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button Width="46" Height="46" Content="—" FontSize="15" Foreground="{StaticResource MutedBrush}" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="Minimize_Click" />
                    <Button Width="46" Height="46" Content="×" FontSize="20" Foreground="{StaticResource MutedBrush}" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="Close_Click" />
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="34,20,34,32">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280" />
                    <ColumnDefinition Width="34" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <Image Source="Assets/logo.png" Width="176" Height="176" HorizontalAlignment="Left" Stretch="Uniform" />
                    <TextBlock Text="MatHax" FontSize="46" FontWeight="Bold" Margin="0,15,0,0" />
                    <TextBlock Text="R E B O R N" FontSize="17" FontWeight="SemiBold" Foreground="{StaticResource MintBrush}" />
                    <TextBlock Text="Legacy soul. New machinery." FontSize="14" Foreground="{StaticResource MutedBrush}" Margin="0,17,0,0" />
                    <TextBlock x:Name="VersionText" Text="Minecraft 26.2 • checking release" FontSize="12" Foreground="{StaticResource MutedBrush}" Margin="0,7,0,0" />
                </StackPanel>

                <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}" CornerRadius="14" Padding="28">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <StackPanel>
                                <TextBlock Text="Account" FontSize="12" Foreground="{StaticResource MutedBrush}" />
                                <TextBlock x:Name="AccountText" Text="Not signed in" FontSize="20" FontWeight="SemiBold" Margin="0,5,0,0" />
                            </StackPanel>
                            <Button x:Name="AccountButton" Content="Sign in with Microsoft" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="AccountButton_Click" />
                        </Grid>

                        <Border Grid.Row="1" Height="1" Background="#302539" Margin="0,23" />

                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Memory" FontSize="12" Foreground="{StaticResource MutedBrush}" />
                                <Slider x:Name="MemorySlider" Minimum="2" Maximum="16" TickFrequency="1" IsSnapToTickEnabled="True" Value="4" Width="260" Margin="0,10,0,0" ValueChanged="MemorySlider_ValueChanged" />
                            </StackPanel>
                            <Border Grid.Column="1" Background="{StaticResource SurfaceHoverBrush}" CornerRadius="8" Padding="13,8" VerticalAlignment="Bottom">
                                <TextBlock x:Name="MemoryText" Text="4 GB" FontWeight="SemiBold" Foreground="{StaticResource MintBrush}" />
                            </Border>
                        </Grid>

                        <StackPanel Grid.Row="3" VerticalAlignment="Center">
                            <TextBlock x:Name="StatusText" Text="Preparing launcher…" FontSize="14" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" />
                            <ProgressBar x:Name="Progress" Height="4" Minimum="0" Maximum="100" Margin="0,15,0,0" Foreground="{StaticResource AccentBrush}" Background="#2A2032" BorderThickness="0" />
                        </StackPanel>

                        <Grid Grid.Row="4" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button x:Name="InstallButton" Content="Install to official launcher" Style="{StaticResource SecondaryButton}" Click="InstallButton_Click" />
                            <Button Grid.Column="2" Content="Open game folder" Style="{StaticResource SecondaryButton}" Click="OpenFolder_Click" />
                        </Grid>

                        <Button x:Name="LaunchButton" Grid.Row="5" Content="Launch MatHax" Height="52" Style="{StaticResource PrimaryButton}" Click="LaunchButton_Click" />
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
